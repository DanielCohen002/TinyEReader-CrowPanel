# Custom partition table for the 8MB flash on this board.
# The stock "8M with spiffs" Arduino scheme gives the app 3MB and LittleFS
# only 1.5MB, even though this firmware compiles to about 1MB. app0 here is
# sized at 1.5MB (leaving headroom to grow) and everything else goes to
# LittleFS -- about 6.3MB instead of 1.5MB.
#
# otadata/ota_0 are kept even though this firmware never does an OTA update:
# the Arduino ESP32 bootloader always expects an otadata slot and uses
# boot_app0.bin (flashed automatically) to point it at ota_0 on first boot.
# Dropping otadata would mean writing a custom bootloader, not just a
# custom partition table.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x180000,
spiffs,     data, spiffs,   0x190000, 0x660000,
coredump,   data, coredump, 0x7F0000, 0x10000,
